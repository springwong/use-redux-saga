import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    button: {
        padding: 16,
        borderColor: 'black',
        borderWidth: 2,
        borderRadius: 8,
        marginTop: 8,
        alignItems: "center",
    },
    displayText: {
        fontSize: 16,
        fontWeight: "600",
        marginTop: 8,
    }
})